/**** DATA TYPES ****/

/*
    The most basic and common data types in JavaScript are
    strings, numbers, and booleans. There are also special types
    like null and undefined. In this exercise you will
    assign values to variables using the let keyword.

    There are also complex types like arrays and objects that serve as
    data structures to store multiple pieces of data, which
    will be covered in the future.
*/

// EXAMPLES

let petName = 'Marta'; // string
let numberOfCats = 2; // number
let hasDogs = false; // boolean
let toCookForDinner = null; // I don't know yet, but there will be something...
let darkMatter; // as yet undefined

// We can prove these have been correctly stored by logging them to
// the console and checking the output when we run the file in the terminal.

console.log(petName);
console.log(numberOfCats);
console.log(hasDogs);
console.log(toCookForDinner);
console.log(darkMatter);

// Depending on your terminal, you may note that numbers and booleans are a different 
// color in the output than strings, and the output of a string is the usual color 
// and is not enclosed in quotes. Similarly, null may appear in bold, and undefined 
// will likely appear as a medium gray. This is normal.

// YOUR TURN!

// For each of the items below:
//     A) Determine the appropriate data type,
//     B) initialize an variable to store an example value, and
//     C) log each variable to the console to verify it.

// The title of your favorite book
let faveBook = "Perelandra, by C.S. Lewis";
console.log(faveBook);

// The number representing your birth month
let myBirthMonth = 11;

// Whether or not you love cilantro
let lovesCilantro = true; 

// How many siblings you have
let numberOfSiblings = 7;

// The exact date you will land your first job in tech after LaunchCode
let futureJobDate = null; // one day you'll have a value for this

// An adjective that describes how you feel about learning to code
let thatCodingFeeling = "excited";

// The result of the evaluation of 2 + 2 === 5
let twoPlusTwoEqualsFive = false;

// Identifying which data type to use in various situations and how
// their syntax differs is an important foundations for learning
// to code. Onward and upward!
